# RAG 项目依赖 - 本地全栈方案（零 API 费用）
# 安装：pip install -r requirements.txt
# 说明：LangChain 1.x 起各集成包独立发布，不会被主包自动带入，必须逐个声明

# Web API 与前端
fastapi>=0.115.0
uvicorn>=0.46.0
python-multipart>=0.0.20
streamlit>=1.40.0

# LangChain 核心与集成
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
langchain-ollama>=1.0.0
langchain-chroma>=1.1.0
langchain-text-splitters>=1.0.0

# Agent 编排与 MCP
langgraph>=1.0.0              # Agentic RAG 的工具调用循环（backend/chains/agent_chain.py）
mcp>=2.0.0                    # 把知识库暴露成 MCP server（backend/api/mcp_server.py）

# 向量数据库
chromadb>=0.5.0

# 文档解析
PyMuPDF>=1.24.0               # PDF 文字层提取（代码里 import fitz）
python-docx>=1.1.0            # DOCX 解析
rapidocr-onnxruntime>=1.3.0   # 扫描件 / 图片 OCR，ONNX 离线；未安装时自动降级
Pillow>=10.0.0                # 图像解码
numpy>=1.26.0

# 混合检索与精排
rank_bm25>=0.2.2              # BM25 关键词召回
jieba>=0.42.1                 # BM25 中文分词，避免默认按单字切
FlagEmbedding>=1.2.10         # bge-reranker 精排

# 推理运行时
torch>=2.5.0                  # FlagEmbedding 后端
onnxruntime>=1.20.0           # RapidOCR 后端，纯 CPU 可跑

# 工具库
pyyaml>=6.0
pydantic>=2.10.0
httpx>=0.28.0
aiofiles>=24.0.0
